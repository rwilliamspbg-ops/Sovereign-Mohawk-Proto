name: Build Verification
on: [push]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23' # Standardize to 1.23
      - name: Verify
        run: echo "Environment verified."
