secrets/mohawk_api_token
libmohawk.so
libmohawk.h
runtime-secrets/
data/

# Python cache artifacts
__pycache__/
*.pyc

# Generated result outputs should be committed only when intentionally curated evidence.
# Use `git add -f <path>` for deliberate snapshots.
results/**
